// Arquivo de configuração que rodará antes de cada teste
// Útil pra fazer mocks globais, limpar banco, etc no futuro.

beforeAll(() => {
  // Setup antes de todos os testes
});

afterAll(() => {
  // Teardown depois de todos os testes
});